
import { urlParser } from 'esapp/helpers/url-parser';
import { module, test } from 'qunit';

module('Unit | Helper | url parser');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = urlParser([42]);
  assert.ok(result);
});

